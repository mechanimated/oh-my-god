import React from 'react'

export default function StartMenu(onStartClick) {
    
  return (
    <div>
        <button onClick={onStartClick}/>
            Start Game
        <button />
    </div>
  )
}
